# Ember nodes: class ids, display names, widget order

Generated by `tests/ember_nodes/node_spec.py` from the live `INPUT_TYPES` (do not hand-edit).
Each node's inputs, order, defaults and limits are identical to the node it replaces.
In a UI workflow, `control_after_generate` follows every `seed` widget.

## `EmberResolutionMP` - Ember Resolution (MP)

Widgets, in order:

| # | widget | type | details | section |
|---|---|---|---|---|
| 1 | `megapixels` | FLOAT | default `0.98`; min 0.05; max 8.0; step 0.01 | required |
| 2 | `aspect_ratio` | COMBO | default `16:9`; options: `from image (input)`, `1:1`, `4:3`, `3:4`, `3:2`, `2:3`, `16:9`, `9:16`, `21:9`, `5:4`, `4:5` | required |
| 3 | `multiple_of` | INT | default `32`; min 1; max 128; step 1 | required |

Link inputs: `image` (IMAGE, optional)
Outputs: `width` (INT), `height` (INT), `aspect_ratio` (STRING)

## `EmberSaveImageNoMetadata` - Ember Save Image (No Metadata)

Widgets, in order:

| # | widget | type | details | section |
|---|---|---|---|---|
| 1 | `filename_prefix` | STRING | default `ComfyUI` | required |
| 2 | `format` | COMBO | default `PNG`; options: `PNG`, `JPEG` | required |
| 3 | `quality` | INT | default `95`; min 1; max 100; step 1 | required |

Link inputs: `images` (IMAGE)
Outputs: none (output node)

## `EmberCameraLook` - Ember Camera Look

Widgets, in order:

| # | widget | type | details | section |
|---|---|---|---|---|
| 1 | `enabled` | BOOLEAN | default `True` | required |
| 2 | `demosaic_pixel_blur` | FLOAT | default `1.0`; min 0.0; max 5.0; step 0.05 | required |
| 3 | `noise_strength` | FLOAT | default `3.0`; min 0.0; max 5.0; step 0.1 | required |
| 4 | `kernel_motion_blur` | FLOAT | default `1.0`; min 1.0; max 51.0; step 0.1 | required |
| 5 | `jpeg_compression` | INT | default `98`; min 85; max 100; step 1 | required |

Link inputs: `image` (IMAGE)
Outputs: `image` (IMAGE)

## `EmberRenoise` - Ember Renoise

Widgets, in order:

| # | widget | type | details | section |
|---|---|---|---|---|
| 1 | `seed` | INT | default `0`; min 0; max 18446744073709551615 | required |
| 2 | `iso_preset` | COMBO | default `ISO 400`; options: `ISO 0 (Off)`, `ISO 50`, `ISO 100 (Clean)`, `ISO 200`, `ISO 400`, `ISO 800`, `ISO 1600`, `Night Mode` | required |
| 3 | `grain_size` | FLOAT | default `1.0`; min 0.1; max 8.0; step 0.1 | required |

Link inputs: `image` (IMAGE)
Outputs: `image` (IMAGE)

## `EmberHDBBoxDetectorProvider` - Ember HD Ultralytics BBox Loader

Widgets, in order:

| # | widget | type | details | section |
|---|---|---|---|---|
| 1 | `model_name` | COMBO | default `bbox/Eyeful_v2-Paired.pt`; options: `bbox/Eyeful_v2-Paired.pt` | required |
| 2 | `imgsz` | INT | default `1280`; min 320; max 2048; step 32 | required |

Link inputs: none
Outputs: `BBOX_DETECTOR` (BBOX_DETECTOR), `SEGM_DETECTOR` (SEGM_DETECTOR)

## `EmberDetailer` - Ember Detailer

Widgets, in order:

| # | widget | type | details | section |
|---|---|---|---|---|
| 1 | `guide_size` | FLOAT | default `512`; min 64; max 16384; step 8 | required |
| 2 | `guide_size_for` | BOOLEAN | default `True` | required |
| 3 | `max_size` | FLOAT | default `1024`; min 64; max 16384; step 8 | required |
| 4 | `seed` | INT | default `0`; min 0; max 18446744073709551615 | required |
| 5 | `steps` | INT | default `20`; min 1; max 10000 | required |
| 6 | `cfg` | FLOAT | default `8.0`; min 0.0; max 100.0 | required |
| 7 | `sampler_name` | COMBO | default `euler`; options: `euler`, `euler_cfg_pp`, `euler_ancestral`, `euler_ancestral_cfg_pp`, `heun`, `heunpp2`, `…`, `(44 options)` | required |
| 8 | `scheduler` | COMBO | default `simple`; options: `simple`, `sgm_uniform`, `karras`, `exponential`, `ddim_uniform`, `beta`, `…`, `(18 options)` | required |
| 9 | `denoise` | FLOAT | default `0.5`; min 0.0001; max 1.0; step 0.01 | required |
| 10 | `feather` | INT | default `5`; min 0; max 100; step 1 | required |
| 11 | `noise_mask` | BOOLEAN | default `True` | required |
| 12 | `force_inpaint` | BOOLEAN | default `True` | required |
| 13 | `bbox_threshold` | FLOAT | default `0.5`; min 0.0; max 1.0; step 0.01 | required |
| 14 | `bbox_dilation` | INT | default `10`; min -512; max 512; step 1 | required |
| 15 | `bbox_crop_factor` | FLOAT | default `3.0`; min 1.0; max 10; step 0.1 | required |
| 16 | `sam_detection_hint` | COMBO | default `center-1`; options: `center-1`, `horizontal-2`, `vertical-2`, `rect-4`, `diamond-4`, `mask-area`, `mask-points`, `mask-point-bbox`, `none` | required |
| 17 | `sam_dilation` | INT | default `0`; min -512; max 512; step 1 | required |
| 18 | `sam_threshold` | FLOAT | default `0.93`; min 0.0; max 1.0; step 0.01 | required |
| 19 | `sam_bbox_expansion` | INT | default `0`; min 0; max 1000; step 1 | required |
| 20 | `sam_mask_hint_threshold` | FLOAT | default `0.7`; min 0.0; max 1.0; step 0.01 | required |
| 21 | `sam_mask_hint_use_negative` | COMBO | default `False`; options: `False`, `Small`, `Outter` | required |
| 22 | `drop_size` | INT | default `10`; min 1; max 16384; step 1 | required |
| 23 | `cycle` | INT | default `1`; min 1; max 10; step 1 | required |
| 24 | `interpolation` | COMBO | default `bicubic`; options: `bicubic`, `bilinear`, `nearest-exact`, `nearest`, `area` | required |
| 25 | `sharpness` | FLOAT | default `0.5`; min 0.0; max 2.0; step 0.05 | required |
| 26 | `color_match_strength` | FLOAT | default `1.0`; min 0.0; max 1.0; step 0.05 | required |
| 27 | `inpaint_model` | BOOLEAN | default `False` | optional |
| 28 | `noise_mask_feather` | INT | default `20`; min 0; max 100; step 1 | optional |
| 29 | `tiled_encode` | BOOLEAN | default `False` | optional |
| 30 | `tiled_decode` | BOOLEAN | default `False` | optional |

Link inputs: `image` (IMAGE), `model` (MODEL), `clip` (CLIP), `vae` (VAE), `positive` (CONDITIONING), `negative` (CONDITIONING), `bbox_detector` (BBOX_DETECTOR, optional), `segs` (SEGS, optional), `sam_model_opt` (SAM_MODEL, optional), `segm_detector_opt` (SEGM_DETECTOR, optional), `detailer_hook` (DETAILER_HOOK, optional), `scheduler_func_opt` (SCHEDULER_FUNC, optional)
Outputs: `image` (IMAGE), `cropped_refined` (IMAGE), `cropped_enhanced_alpha` (IMAGE), `mask` (MASK), `detailer_pipe` (DETAILER_PIPE), `cnet_images` (IMAGE)

## `EmberSpeedHDSampler` - Ember Speed HD Sampler

Widgets, in order:

| # | widget | type | details | section |
|---|---|---|---|---|
| 1 | `base_sampler` | COMBO | default `euler`; options: `ar_video`, `ddpm`, `deis`, `dpm_2`, `dpm_2_ancestral`, `dpm_2_ancestral_RF`, `…`, `(42 options)` | required |
| 2 | `transform` | COMBO | default `dct`; options: `dct`, `dwt`, `fft` | required |
| 3 | `mode` | COMBO | default `delta_optimal`; options: `delta_optimal`, `manual` | required |
| 4 | `model_preset` | COMBO | default `flux`; options: `flux`, `wan21`, `custom` | required |
| 5 | `scales` | STRING | default `0.5,1.0` | required |
| 6 | `delta` | FLOAT | default `0.01`; min 0.0001; max 0.5; step 0.001 | required |
| 7 | `manual_sigmas` | STRING | default `0.85` | required |
| 8 | `spectrum_A` | FLOAT | default `203.615097`; min 0.0; max 1000000.0; step 0.001 | required |
| 9 | `spectrum_beta` | FLOAT | default `1.915461`; min 0.0; max 10.0; step 0.001 | required |
| 10 | `seed` | INT | default `0`; min 0; max 2147483647; step 1 | required |

Link inputs: none
Outputs: `sampler` (SAMPLER)

`base_sampler` lists ComfyUI's `comfy.k_diffusion.sampling.sample_*` solvers minus dpm_fast, dpm_adaptive and lcm, read at run time (42 on ComfyUI v0.34.6), exactly as the reference node does.

